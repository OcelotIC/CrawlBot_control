# PHASE CLEANUP-27 — test-suite audit, test by test

**Question:** with the gate proving the canonical run byte-identical, is the
test suite still needed?

228 collected tests / 180 test functions / 19 files, inventoried individually
(`gate/_run/test_inventory.tsv`, generated by `gate/_run/test_inventory.py`).

**Answer: yes — but a quarter of it should go.** The reasoning below is the
point; the recommendation falls out of it.

---

## 1. First, what the gate can and cannot prove

The gate replays **one config** and compares 2 077 rows × 132 928 fields to a
frozen baseline. On that config it is close to unbeatable: it caught an injected
1e-10 change at row 11.

What it structurally **cannot** do:

| | why |
|---|---|
| prove a physics identity | it proves *reproducibility*, not *correctness*. A wrong-but-stable change passes. |
| exercise another config | `r_com` at 3 m, tight rate bounds, `torso_mass=0`, inverted `hw` bounds — the canonical visits none of these |
| exercise a non-canonical branch | 5 of 6 AOCS laws, `_solve_strict`, 6 `ik` functions, all of `diagnostics/` |
| check error handling | NaN input, wrong-size `x0`, infeasible solve, stale warm start |

Those four gaps are exactly where the suite earns its place — or fails to.

---

## 2. The suite, classified

### A. Physics and mathematical identities — **keep, highest value**

These assert truths, not behaviour. The gate cannot express them.

| file | tests | what it pins |
|---|---:|---|
`test_invariants.py` | 16 | `H` symmetric + positive-definite (nominal + 5 random configs); `v_com == J_com @ v`; `h_centroidal[0:3] == m·v_com`; `L_com == h_centroidal[3:6]`; `skew` antisymmetry and `skew(v)@w == v×w`; momentum-map shape and zero columns for inactive contacts; contact-Jacobian shapes for each phase
`test_momentum.py` | 6 | **`H_{r/O} = L_com + r_com × (m·v_com)` exactly** — the identity the entire paper rests on — across multiple configs; orbital term grows with offset; zero velocity ⇒ zero momentum
`test_mapping_layer.py` | 12 | **the T3 Jacobian equivalence**: `J_b_pos = (m_tot/m_b)·J_com − (1/m_b)·Σ mᵢJᵢ`, at neutral and **10 random configs**, plus the centroidal cross-check. This is the mathematical licence to track the torso instead of the CoM — the whole reason `com_to_torso_mapping` exists
`test_frame_conversions.py` | 9 | round-trip identity with and without structure rotation; **the wxyz↔xyzw quaternion convention**; the ω_s Coriolis term; RWA (nq=29) vs non-RWA (nq=26) agreement; gimbal-lock has no NaN
`test_contact_dynamics.py` | 11 | `ContactConfig.from_phase` for all three phases; momentum-map structure and symmetry; gait-plan timing invariant `t_end[-1] == total`; weld equalities exist in the MJCF

**54 tests** (16+6+12+9+11). `test_frame_conversions` deserves special note: it guards the exact
trap CLAUDE.md warns about ("do not assume quaternion conventions"), and a silent
failure there would corrupt every number in the paper while the gate stayed green
— because the gate would faithfully reproduce the *wrong* convention.

### B. Component behaviour off the canonical trajectory — **keep**

| file | tests | what it reaches that the canonical does not |
|---|---:|---|
`test_coarse_preplanner.py` | 19 | the momentum box at **every** knot, the κ=0.7 terminal margin, wrench bounds, the rate bound, and the **position-dependent envelope at r = 1 m vs 3 m** — the canonical only ever visits its own standoff |
`test_aocs_physics.py` | 15 | `H_rO` computation; the transport term `ω_s × H`; **the K_θ sign convention**; that damping actually *reduces* ω_s; the PID→PD reduction at `K_θ=0`. Covers AOCS laws the canonical never selects |
`test_planners_6d.py` | 16 | delayed-cosine properties: `σ̇=0` at both boundaries, **continuity at τ_d**, rotation concentrated in the second half; SLERP endpoints; `L_com_ref == I·ω` at the peak |
`test_contact_estimator.py` | 11 | GMO convergence to `τ_ext` and time constant ≈ `1/K_O`; **the full state-machine transition sequence, hysteresis and debounce** — and the state machine is *inert* on the canonical, so these tests are its only verification |
`test_nmpc_qp_consistency.py` | 10 | convergence from nominal and from a 10 % perturbation; `L_com` bounds; **cold vs warm start equivalence**; infeasibility reported |
`test_aocs_orbital.py` | 5 | the orbital product rule `d/dt(r × m·v) = r × m·v̇` — numerically, against the wrong form |
`test_nmpc_conservation.py` | 3 | the M3 conservation-law tests: hw box from rest, far step takes longer, terminal margin within κ |

**79 tests.**

### C. Robustness and named regression guards — **keep**

| file | tests | what it guards |
|---|---:|---|
`test_liabilities.py` | 16 | six named liabilities L1–L6: NaN input must not silently succeed; wrong-size `x0` must raise; **the warm start must survive a failed solve** — this is fix F2, the bug the chantier found; `torso_mass=0` must not crash; config gaps (non-integer `dt` ratio, inverted `hw` bounds, `dt_qp=0`) |
`test_ik_anomaly_regression.py` | 4 | **the Phase-4 step-2 anomaly (`w_end = 4e-8`) must not recur**; determinism of the same call twice; ≥6 multi-start seeds |
`test_sim_log_environment.py` | 1 | the environment fingerprint round-trips — the thing that makes byte-identity meaningful |

**21 tests.** `test_liabilities::test_warm_start_survives_failed_solve` is worth
singling out: it is the standing guard on a bug this chantier fixed. Deleting the
suite would delete the proof that F2 stays fixed.

### D. Testing removed or disabled features — **retire**

| file | tests | verdict |
|---|---:|---|
`test_mid_waypoint_reshape.py` | 4 | **3 retire** (`FileNotFoundError` — fixtures for the reshape path removed in CLEANUP-15). **1 keep**: `test_torso_planner_piecewise_continuous` checks live TorsoPlanner C0 continuity and `v=a=0` at the seam — move it to `test_planners_6d.py` |
`test_reworked_qp.py` | 8 | **all 8 failing — one cause, diagnosed in §4:** the shared `_build_qp` helper passes 9 kwargs this chantier deleted from `WholeBodyQPConfig`. `TypeError` at `tests/test_reworked_qp.py:87`; no test body runs. They passed before CLEANUP-6 |
`test_trajectory_aware_ik.py` | 4 | passes, but exercises `manipulability_config_trajectory` / `precompute_torso_map` — orphaned from `sim_loop` by CLEANUP-15. **The only coverage of those functions.** Keep *only* if those functions are kept |
`test_diagnostics.py` | 10 | smoke tests for `crawlbot/diagnostics/`, which the canonical never runs. **The only thing exercising that package.** Its fate follows the package's (CLAUDE.md rule 3 question) |

**26 tests, of which 11 are the clear retirees** (3 + 8).

### E. One failure that is a genuine open question

`test_coarse_preplanner::test_far_infeasible_under_tight_rate` — CLAUDE.md
already flags *"re-examine its semantics at cap 2.5"*. It asserts a far step
becomes infeasible under a tight rate bound; the frozen cap **is** 2.5 now, so
the premise may have moved. Diagnose, do not delete.

---

## 3. Coverage: what the suite reaches and the gate does not

Measured by running both under `coverage.py` and differencing per line
(`gate/_run/cov_compare.py`): the canonical replay vs the full pytest suite,
over `crawlbot/`'s 6 479 statements.

| | statements | share |
|---|---:|---:|
| covered by **both** | 1 900 | 29 % |
| **gate only** | 1 701 | 26 % |
| **suite only** | **1 039** | **16 %** |
| **neither** | 1 839 | 28 % |

So the suite does reach 1 039 statements the canonical never executes. **But
where it reaches them changes the argument**, and honesty requires saying so:

| module | suite-only | note |
|---|---:|---|
| `diagnostics/plots.py` | 412 | |
| `diagnostics/metrics.py` | 196 | |
| `diagnostics/runner.py` | 18 | |
| **subtotal `diagnostics/`** | **626** | **60 % of the suite's unique coverage** |
| `core/ik.py` | 186 | the 6 functions orphaned by CLEANUP-15 |
| `aocs/force_estimator.py` | 72 | the 5 non-canonical AOCS laws |
| `estimation/contact_estimator.py` | 49 | the inert `ContactStateMachine` |
| everything else | 106 | |

**60 % of the suite's unique reach is `crawlbot/diagnostics/`** — a package the
canonical never runs and whose own fate is undecided (CLAUDE.md rule 3). If that
package goes, the suite's unique coverage falls from 1 039 to ~413 statements
(6 %). The "16 %" is real but it is not evenly earned.

### Two findings the coverage diff produced on its own

**`wholebody_qp.py`: suite-only = 0.**

```
stmts 330 | gate 319 | suite 63 | both 63 | suite-only 0 | gate-only 256
```

The 8 `test_reworked_qp` tests add **zero reach**. Read at the time as "every
line they touch, the canonical already executes" — §4 shows the real reason:
**they crash in the fixture, so no test body executes at all.** The 63 lines the
suite does reach are import-time and other files' incidental use. The correct
statement is not "their value is assertions rather than coverage" — it is that
they currently assert nothing, and `WholeBodyQP` has **no live component test**.

**`_solve_strict` is covered by nothing, and `CARRYOVER` §B2 is wrong about why
it is kept.** §B2 justifies keeping it as having *"2 test users + 6 script
users"*. Measured:

```
_solve_strict   0/76 lines covered by the suite   (and 0 by the gate)
grep for _solve_strict | 'strict' across the WHOLE repo, Misc included:
  -> one hit, the docstring of WholeBodyQPConfig.method
```

**Zero consumers anywhere.** 76 statements, the largest single block verified by
nothing, kept on a justification that does not hold. Fifth instance in this
chantier of an inherited claim failing on measurement — and this one was found by
a tool built for a different purpose, which is the argument for having the tools.

### The 1 839 statements covered by neither

| module | uncovered | |
|---|---:|---|
| `simulation/sim_loop.py` | 808 | mostly the non-canonical branches of `_step` |
| `core/ik.py` | 388 | |
| `planning/sequence_loader.py` | 107 | the `.seq` path — a real user feature with **no** verification |
| `simulation/plotting.py` | 101 | |
| `solvers/hierarchical_qp.py` | 67 | `_solve_strict` |
| `diagnostics/plots.py` | 64 | |
| `planning/locomotion_planner.py` | 60 | on the removal list |

`sequence_loader.py` is the one worth flagging: 107 statements, a documented
user-facing feature (`sim.setup(sequence_path=…)`), and **nothing tests it**. It
was kept in CLEANUP-16 on the correct principle that unused ≠ retired — but
"kept" and "verified" are different things, and it is currently neither.

---

## 4. The `test_reworked_qp.py` question — answered

This section originally listed three possibilities per test — stale thresholds,
feature-off, or a real defect — and called separating them "the next piece of
work". Asked for links to the 8 tests, running them printed the answer in one
line. **None of the three is right.**

### One cause, eight reported failures

```
E  TypeError: WholeBodyQPConfig.__init__() got an unexpected keyword argument 'alpha_com'
   tests/test_reworked_qp.py:87
```

Identical for all 8. They share one helper, `_build_qp`, and it dies constructing
the config — line 87, the first kwarg. **No test body executes.** The 8 entries in
the failure list are 8 collections of the same crash, not 8 findings.

Diffing the helper's kwargs against the live dataclass, **9 of 11 named fields no
longer exist**:

```
MISSING: alpha_com  alpha_com_soft  alpha_torso  alpha_torso_ang
         cooperative_arms_mode  ee_null_space  use_m2_stack
         ss_centroidal_momentum_task  ss_alpha_tl_weak
PRESENT: alpha_ee  alpha_posture  alpha_reg  alpha_torque  alpha_wrench
         alpha_passivity  ss_alpha_mom  tau_max  L_max  dt_qp  nq
```

Those 9 are the M2 / cooperative-split / pre-two-task config surface, removed by
**CLEANUP-6 `4e2e8da`** ("excise the legacy pre-two-task SS task stack") and
**CLEANUP-9 `772b556`**. The tests were built on it and were never repointed.

### Measured, not inferred: they passed before CLEANUP-6

Clean worktree at `4e2e8da^` (= `7f60403`), same interpreter and env:

```
$ pytest tests/test_reworked_qp.py -q
8 passed in 95.28s

$ pytest tests/test_coarse_preplanner.py -q
1 failed, 18 passed          <- test_far_infeasible_under_tight_rate
```

So of the 9 "diagnose then decide" tests:

| | verdict |
|---|---|
| `test_reworked_qp` ×8 | **this chantier broke them.** Regression from CLEANUP-6/9, not pre-existing |
| `test_far_infeasible_under_tight_rate` | **genuinely pre-existing** — fails at `4e2e8da^` too. Already on CLAUDE.md's Remaining Work as an envelope-semantics question at cap 2.5 |

### The correction this forces

CLEANUP-25 §4 and CLAUDE.md both called all 12 suite problems "pre-existing",
attributing *at least four* to the chantier. The true count is **at least
twelve**: 3 `mid_waypoint_reshape` + 1 `fk_reference_consistency` + **these 8**.
Only `test_far_infeasible_under_tight_rate` predates the chantier.

Rule 7 says re-run the suite after modifying a core module and fix what breaks.
CLEANUP-6 removed 9 config fields and did not. The failure then propagated as a
*claim*: "pre-existing" was written down once and inherited three reports deep,
which is the exact pattern this chantier keeps finding. **Sixth instance**, and the
first where the false inherited claim was mine rather than the repository's.

### What the fix actually is

Not "diagnose 8 tests". Two decisions on one helper:

- **T7 / T8 / T9 / T10** drive the M2 stack (`use_m2_stack=True`, `ee_null_space`,
  `alpha_com_soft`) — a removed architecture. **Retire**, same class as
  `test_fk_reference_consistency` in CLEANUP-26: repairing them means restoring
  the feature.
- **`TestPhase20TMOM` ×4** test the **T-MOM task, which is canonical and live**
  (α = 400). They reach it through the removed `cooperative_arms_mode` scaffolding,
  but the task under test is real. **Port the helper to the two-task API** — that
  is the one action here that would give `wholebody_qp.py` its first working
  component test, and it is worth doing for exactly the reason §3 gives: the
  module the paper rests on currently has none.

> **Refined by CLEANUP-28** (`PHASE_CLEANUP_28_TEST_REWORKED_QP_PORT.md`): the
> retire/port split above was drawn from which *mechanism* each test named, and
> that over-retires. Measured on the current stack, **6 of the 8 assertions hold
> at their original thresholds** — including T7 (torso 1.800 mm / EE 0.481 mm /
> 0.318°), T9 (residual 8.9e-16) and T10 (0 passivity violations in both DS
> modes). Only **T8** (soft-CoM residual, task deleted) and **T-MOM/4** (Variant B,
> `ss_alpha_tl_weak`) are unportable. A test whose numbers survive a
> re-architecture is testing physics, not architecture — the mechanism it was
> written against is the wrong thing to retire it on.

---

## 5. Recommendation

**Keep the suite.** It covers what the gate structurally cannot: physics
identities, off-canonical configs, non-canonical branches, and error handling.
Deleting it would leave the repository with excellent *reproducibility* and no
*correctness* checking at all — and the chantier has already shown that a
confident wrong claim survives indefinitely when nothing measures it.

Concretely:

| action | tests |
|---|---:|
| **keep as-is** (A + B + C) | **154** |
| **retire** — `test_mid_waypoint_reshape` ×3, plus `test_reworked_qp` T8 + T-MOM/4 (subject deleted; CLEANUP-28 §2) | 5 |
| **port to the two-task API** — the other 6 `test_reworked_qp`: all 6 hold at their original thresholds (CLEANUP-28 §3). **Done** | 6 |
| **move** — `test_torso_planner_piecewise_continuous` → `test_planners_6d.py` | 1 |
| **open question** — `test_far_infeasible_under_tight_rate`, the one genuinely pre-existing failure | 1 |
| **fate follows their subject** — `test_diagnostics` (10), `test_trajectory_aware_ik` (4) | 14 |

Doing the port and the retirements takes the suite from 9 failures to 1, and that
1 is the only one that was ever a question about the controller.

**And gate it.** Once the retirees are gone and §4's port is done, the suite should
be green and then *enforced* — otherwise it drifts again, exactly as it drifted
from a documented "2 failures" to a measured 12 without anyone noticing. A suite
nobody runs is not a safety net; it is a claim about a safety net.

Suggested split, since a 10-minute suite will not be run per-commit:
- **per-commit**: classes A + C (identities and robustness) — fast, no IPOPT
- **pre-merge**: the whole suite plus the gate
