__pycache__/
*.pyc
*.pyo

# Editor files
*.swp
*.swo
*~
.vscode/
.idea/

# OS files
.DS_Store
Thumbs.db

# Test outputs (generated at root by some tests)
/*.json
/*.png
results/benchmarks/*.json

# Scratch from ad-hoc diagnostic checks (regenerable from scripts + committed sim_logs)
results/_diag_check/
scripts/_diag_check_*.py

# Bulky machine-readable run artifacts (regenerable by re-running the diag
# scripts). Deliverable figures (*.png referenced by docs) are committed
# explicitly; these logs are intermediate and bloat the repo.
results/**/sim_log.json
results/**/step_log.json
results/**/nmpc_step_log.json
results/**/hw_slack_log.json
results/**/physics_trace.pkl
results/**/*_metrics.txt
results/**/step_metrics.txt
results/**/sat_stats.txt

# One-off exploratory run: v21 + mapping-bypass + raised EE-ang gains.
# Numerics reported in chat; not preserved on branch per instruction.
results/M7_1pct_1step_v21_mapping_off_hi_ori_gain/

# v22 sweep sample: swing_early_finish_fraction=0.70 (primary run is 0.80,
# preserved on-branch in results/M7_1pct_1step_v22_with_swing_hold/).
results/M7_1pct_1step_v22_hold_070/
scripts/_run_m7_v22_hold_070.py

# T15 mid-waypoint validation run artifacts (Phase 7 of step2-path-diagnostic).
# Per the prompt brief: report (T15_midwaypoint_report.md) is committed; the
# heavy run outputs (sim_log, physics_trace, metrics, ik_trace, figs) are not.
results/M7_1pct_3step_v22_t15_midwaypoint/sim_log.json
results/M7_1pct_3step_v22_t15_midwaypoint/physics_trace.pkl
results/M7_1pct_3step_v22_t15_midwaypoint/metrics.csv
results/M7_1pct_3step_v22_t15_midwaypoint/ik_trace.json
results/M7_1pct_3step_v22_t15_midwaypoint/fig*.png

# Regenerable raw sim-run dumps from the §VII ablation / h_max-frontier / QP-cond
# phases (these dirs carry heavy sim_log.json + traces). The tidy exports, analysis
# JSONs, and reports ARE committed elsewhere (e.g. results/j2_ablation_envelope/,
# results/j2_canonical_revalidation/). Already ignored on j2/ablation-envelope; this
# aligns j2/ds-active-rework with that convention. (ADJ-CONV reads figC_qpcond's
# snapshots but its tidy deliverable lives under results/j2_adjconv/.)
results/figU_rateoff/
results/figC_nominal/
results/figC_qpcond/
results/figC_qpcond6/
results/figC_hmax6/
results/figC_hmax6_ee/
results/figC_hmax6_tmax/
results/figC_hmax7/
results/_canon5_control/
results/_frontier_cap/

# PREPLAN-FIX revalidation raw run dumps (regenerable; before/after in
# results/j2_adjconv/PHASE_PREPLAN_FIX_STEP2.md)
results/figC_preplanfix/
results/figU_preplanfix/

# STEP5-MARGIN calibration raw run dumps (regenerable)
results/figC_step5cal/
results/figC_step5iso/
results/figC_step5fix/

# TSTEP-DIAG-ALL per-step sweep raw run dumps (regenerable)
results/figC_sw_*/

# EE-RATIO-GLOBAL torso:EE ratio sweep raw run dumps (regenerable; table in
# results/j2_adjconv/PHASE_EE_RATIO_GLOBAL.md + ratio_sweep.json)
results/figC_ratio_*/

# REG-DIAG epsilon-sweep raw run dumps (regenerable; table in
# results/j2_adjconv/PHASE_REG_DIAG.md + reg_sweep.json)
results/figC_reg_*/

# User-weight quick-try raw run (regenerable; result in results/j2_adjconv/userweights_result.json)
results/figC_userw*/

# HIER-STANDARD-SWEEP raw run dumps (regenerable; tables in results/j2_adjconv/PHASE_HIER_STANDARD_SWEEP.md)
results/figC_hier_*/

# COPRIORITY-1000 raw run (regenerable; result in results/j2_adjconv/copri1000_result.json)
results/figC_copri*/
